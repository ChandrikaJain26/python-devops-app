# python-devops-app

A small Flask API designed for DevOps practicals:
- Wheel build (pyproject.toml)
- Unit tests (pytest)
- Functional test (curl /health)
- Performance tests (JMeter hitting /health)
- Docker build
